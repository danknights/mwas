# Test cross validation function
# -------
# Input: 
#     x : feature vector (sample x features)
#     y : desired response
# nflods: number of folds
# classifier: classifier type
#
#--------
# Function of cross-validation

"test.cross.validation" <- function(x, y, nfolds=10, classifier = c("RF","SVM", "knn", "MLR")[1], ...) {
  
  
}
